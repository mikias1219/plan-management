services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - lifeos-mongo:/data/db
    environment:
      MONGO_INITDB_DATABASE: lifeos

volumes:
  lifeos-mongo:
